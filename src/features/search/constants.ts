export const MIN_SEARCH_QUERY_LENGTH = 2;

export const DEFAULT_SEARCH_PAGE = 0;

export const DEFAULT_SEARCH_SIZE = 20;

export const MODAL_PREVIEW_SIZE = 3;

/** Как в каталоге; делится без остатка и на 3, и на 4 колонки сетки. */
export const SEARCH_PAGE_SIZE = 12;

export const SEARCH_DEBOUNCE_MS = 300;

export const RECENT_SEARCHES_LIMIT = 5;
