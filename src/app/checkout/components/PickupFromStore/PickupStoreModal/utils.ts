// Резервный центр карты — используется только пока не загрузился список
// винотек текущего города (см. PickupStoreMap.tsx: как только он приходит,
// карта сама центрируется на первой доступной винотеке этого города).
// Координаты — географический центр РФ (Красноярский край), нейтральная
// точка, не привязанная к конкретному городу.
export const DEFAULT_MAP_CENTER: [number, number] = [66.4166, 94.25];
export const DEFAULT_MAP_ZOOM = 12;
export const SELECTED_STORE_ZOOM = 14;

// Цвета маркеров на карте
export const MARKER_COLORS = {
  SELECTED: '#8B2323',
  GRAY: '#6B6B6B',
  BLACK: '#1A1A1A',
};

