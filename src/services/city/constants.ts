/** Имя cookie с выбранным городом (slug). Читается на сервере (SSR) и на клиенте. */
export const CITY_COOKIE = 'city';

/** Год жизни cookie выбора города. */
export const CITY_COOKIE_MAX_AGE = 60 * 60 * 24 * 365;
